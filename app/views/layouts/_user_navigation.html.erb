<ul class="nav_action_non_auth">
  <% if user_signed_in? %>
    <li><%= welcome_message(current_user) %></li>
    <li><%= link_to t('.tests'), tests_path %></li>
    <li><%= link_to t('.logout'), destroy_user_session_path , method: :delete %></li>
  <% else %>
    <li><%= link_to t('.registration'), new_user_registration_path %></li>
    <li><%= link_to t('.sign_in'), new_user_session_path %></li>
  <% end %>
</ul>
