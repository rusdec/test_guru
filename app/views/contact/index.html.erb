<%= page_header(t('.header')) %>

<div class="row justify-content-center">
  <div class="col-md-6">
  <%= form_with url: contact_path, local: true do |form| %>
    <div class="form-group">
      <%= form.text_field :email,
                          class: "form-control form-control-sm",
                          #placeholder: email_field_placeholder(@user),
                          placeholder: t('.email'),
                          value: @user.email %>
    </div>
    <div class="form-group">
      <%= form.text_area :message, class: "form-control form-control-sm", rows: 6 %>
    </div>
    <div class="text-center">
    <%= form.submit t('.send'), class: "btn btn-outline-primary btn-sm " %>
    </div>
  <% end %>
  </div>
</div>

