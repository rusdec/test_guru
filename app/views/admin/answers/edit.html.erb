<%= page_header(answer_header(@answer)) %>
<div class="row justify-content-center">
  <div class="col-md-12 text-center mb-5 text-muted">
    <%= tag.h5 @answer.question.body %>
  </div>
  <%= render 'form', answer: @answer %>
</div>
