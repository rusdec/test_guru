<%= page_header(t('.header')) %>

<div class="row">
  <div class="col-md-12">
    <table class="table table-hover small">
      <thead>
        <tr class="text-center">
          <th><%= t('.title') %></th>
          <th colspan="2"><%= t('.actions') %></th>
        </tr>
      </thead>
      <tbody>
        <%= render @categories %>
      </tbody>
    </table>
    <%= link_to t('.new_category'), new_admin_category_path, class: 'btn light-button' %>
  </div>
</div>
