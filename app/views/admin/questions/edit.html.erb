<%= page_header(question_header(@question)) %>

<div class="row justify-content-center">
  <div class="col-md-12 text-center mb-5 text-muted">
    <%= tag.h5 @question.test.title %>
  </div>
  <%= render 'form' %>
</div>
